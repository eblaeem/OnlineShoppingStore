@using OnlineShoppingStore.Domain.Entities.HomePage.MainSlider;
@using OnlineShoppingStore.ViewModels.Home;
@model HomeIndexViewModels;

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    int _sliderCount = 1;
}

<!-- Start Main Banner -->
<div id="template-mo-zay-hero-carousel" class="carousel slide" data-bs-ride="carousel">
    <ol class="carousel-indicators">
        <li data-bs-target="#template-mo-zay-hero-carousel" data-bs-slide-to="0" class="active"></li>
        @for (int i = 1; i <= Model.Slider.Count - 1; i++)
        {
            <li data-bs-target="#template-mo-zay-hero-carousel" data-bs-slide-to="@i"></li>
        }

    </ol>
    <div class="carousel-inner">
        @foreach (var item in Model.Slider.Where(p => p.LocationEnum == Location.Top).ToList())
        {
            if (_sliderCount == 1)
            {
                <div class="carousel-item active">
                    <div class="container">
                        <div class="row p-5">
                            <div class="mx-auto col-md-8 col-lg-6 order-lg-last">
                                <img class="img-fluid" src="~/@item.Src">
                            </div>
                            <div class="col-lg-6 mb-0 d-flex align-items-center rtl justify">
                                <div class="text-align-left align-self-center">
                                    <h1 class="h1 text-success">@item.Title</h1>
                                    <h3 class="h2">@item.PreTitle</h3>
                                    <p>
                                        @item.Paragraph
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
            else
            {
                <div class="carousel-item">
                    <div class="container">
                        <div class="row p-5">
                            <div class="mx-auto col-md-8 col-lg-6 order-lg-last">
                                <img class="img-fluid" src="~/@item.Src">
                            </div>
                            <div class="col-lg-6 mb-0 d-flex align-items-center rtl justify">
                                <div class="text-align-left align-self-center">
                                    <h1 class="h1">@item.Title</h1>
                                    <h3 class="h2">@item.PreTitle</h3>
                                    <p>
                                        @item.Paragraph
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

            }

            _sliderCount++;
        }

    </div>
    <a class="carousel-control-prev text-decoration-none w-auto ps-3" href="#template-mo-zay-hero-carousel" role="button" data-bs-slide="prev">
        <i class="fa fa-angle-left"></i>
    </a>
    <a class="carousel-control-next text-decoration-none w-auto pe-3" href="#template-mo-zay-hero-carousel" role="button" data-bs-slide="next">
        <i class="fa fa-angle-right"></i>
    </a>
</div>
<!-- End Main Banner-->

<!-- Start Categories of The Month -->
<section class="container py-5">
    <div class="row text-center pt-3">
        <div class="col-lg-6 m-auto">
            <h1 class="h1">Categories of The Month</h1>
            <p>
                Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia
                deserunt mollit anim id est laborum.
            </p>
        </div>
    </div>
    <div class="row">
        @foreach (var item in Model.Slider.Where(p=>p.LocationEnum == Location.Center).ToList())
        {
        <div class="col-12 col-md-4 p-5 mt-3">
            <a href="~/products/details/@item.Id">
            <img src="~/@item.Src" class="rounded-circle img-fluid border">
            </a>
            <h5 class="text-center mt-3 mb-3">@item.Paragraph</h5>
            <p class="text-center"><a class="btn btn-success" href="~/products/details/@item.Id">خرید</a></p>
        </div>
        }
        

    </div>
</section>
<!-- End Categories of The Month -->
